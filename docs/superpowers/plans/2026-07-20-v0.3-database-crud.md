# v0.3 Database CRUD Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** Ship v0.3 with correct REST paths and database-backed CRUD generation while preserving lean in-memory output.

**Architecture:** Keep the existing renderer structure. Add tests first, then make `render_controller` stop double-pluralizing paths and make `render_service_impl` select database-backed or in-memory Java based on feature flags.

**Tech Stack:** Python 3.11, pytest-style tests, generated Java 21, Spring Boot 3.3.x, MyBatis-Plus.

---

### Task 1: Route Pluralization

**Files:**
- Modify: `tests/test_generator.py`
- Modify: `springboot_project_generator/core.py`

- [x] Add a failing test that `ProductController` uses `@RequestMapping("/api/products")` and does not contain `productses`.
- [x] Run the focused test and confirm it fails on the current implementation.
- [x] Update controller path rendering to use `table_name(entity.name).replace("_", "-")`.
- [x] Re-run the focused test and confirm it passes.
- [x] Preserve PascalCase entity names such as `BorrowRecord`.

### Task 2: Database CRUD Renderer

**Files:**
- Modify: `tests/test_generator.py`
- Modify: `springboot_project_generator/core.py`

- [x] Add a failing test for default MyBatis-Plus projects asserting generated `ServiceImpl` injects the mapper and uses `insert`, `selectById`, `selectPage`, `updateById`, and `deleteById`.
- [x] Run the focused test and confirm it fails because the current implementation uses `ConcurrentHashMap`.
- [x] Add a database-backed service implementation renderer and dispatch to it when both `mysql` and `mybatis_plus` are enabled.
- [x] Re-run the focused test and confirm it passes.

### Task 3: Lean Mode Preservation

**Files:**
- Modify: `tests/test_generator.py`
- Modify: `springboot_project_generator/core.py`

- [x] Extend the lean-mode test to assert `ConcurrentHashMap` remains in lean `ServiceImpl`.
- [x] Run the focused test and confirm it guards the existing lean behavior.
- [x] If needed, keep lean dispatch explicitly tied to disabled `mysql` or `mybatis_plus`.
- [x] Re-run the focused test and confirm it passes.

### Task 4: Docs and Version

**Files:**
- Modify: `pyproject.toml`
- Modify: `README.md`
- Modify: `templates/project/README.md.tpl`
- Modify: `skills/springboot-project-generator/SKILL.md`
- Modify: `tests/test_repository_files.py`

- [x] Add failing tests that expect version `0.3.0` and docs describing database-backed CRUD.
- [x] Run the focused tests and confirm they fail on v0.2 docs.
- [x] Update package metadata and docs.
- [x] Re-run focused tests and confirm they pass.

### Task 5: Full Verification

**Files:**
- Test all modified behavior.

- [x] Run Python tests with an available pytest runtime.
- [x] Run CLI generation smoke for default and lean projects.
- [x] If Maven is available, run `scripts/ci-smoke.sh all`; otherwise report that Maven verification was not completed locally.
- [x] Exclude Mockito from generated starter-test dependencies if local or CI Maven smoke fails on Byte Buddy self-attach.
